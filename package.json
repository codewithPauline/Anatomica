{
  "name": "anatomica",
  "version": "0.3.9",
  "private": true,
  "type": "module",
  "scripts": {
    "dev": "vite",
    "build": "vite build",
    "preview": "vite preview",
    "validate:clinical": "node scripts/validate_clinical_cases.mjs",
    "validate:progress": "node scripts/validate_progress_store.mjs",
    "validate:mastery": "node scripts/validate_mastery_dashboard.mjs",
    "validate:remediation": "node scripts/validate_remediation.mjs",
    "validate:variants": "node scripts/validate_challenge_variants.mjs",
    "validate": "npm run validate:clinical && npm run validate:progress && npm run validate:mastery && npm run validate:remediation && npm run validate:variants",
    "assets:extract": "python scripts/prepare_bodyparts3d.py",
    "assets:build": "python scripts/prepare_bodyparts3d.py --convert"
  },
  "dependencies": {
    "three": "^0.179.1"
  },
  "devDependencies": {
    "vite": "^7.1.3"
  }
}
