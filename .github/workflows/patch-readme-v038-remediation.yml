name: Apply Anatomica v0.3.8 README Patch

on:
  push:
    branches: [main]
    paths:
      - '.github/workflows/patch-readme-v038-remediation.yml'

permissions:
  contents: write

jobs:
  patch:
    runs-on: ubuntu-latest
    steps:
      - name: Checkout
        uses: actions/checkout@v4
        with:
          fetch-depth: 0

      - name: Apply README patch
        run: python scripts/patch_readme_v038_remediation.py

      - name: Remove one-time migration files
        run: git rm .github/workflows/patch-readme-v038-remediation.yml scripts/patch_readme_v038_remediation.py

      - name: Commit migration
        run: |
          git config user.name "github-actions[bot]"
          git config user.email "41898282+github-actions[bot]@users.noreply.github.com"
          git add -A
          git commit -m "docs: document targeted remediation"
          git pull --rebase origin main
          git push origin HEAD:main
