name: Apply Anatomica v0.3.9 README Patch

on:
  push:
    branches: [main]
    paths:
      - '.github/workflows/patch-readme-v039-variants.yml'

permissions:
  contents: write

jobs:
  patch:
    runs-on: ubuntu-latest
    steps:
      - name: Checkout
        uses: actions/checkout@v4
        with:
          token: ${{ secrets.GITHUB_TOKEN }}

      - name: Apply README patch
        run: python scripts/migrate_readme_v039.py

      - name: Remove one-time migration files
        run: |
          rm scripts/migrate_readme_v039.py
          rm .github/workflows/patch-readme-v039-variants.yml

      - name: Commit migration
        run: |
          git config user.name "github-actions[bot]"
          git config user.email "41898282+github-actions[bot]@users.noreply.github.com"
          git add -A
          git commit -m "docs: document alternate challenge vignettes"
          git push
